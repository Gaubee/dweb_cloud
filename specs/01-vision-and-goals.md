# Vision And Goals

## 文档状态

- Status: Active
- Scope: 全项目愿景、阶段目标、非目标

## 项目愿景

`dwebCloud` 是一个带 Web3 密钥身份的应用存储云内核。

目标：

- 让纯前端应用可以接入私有同步空间
- 让服务端不持有用户明文数据
- 让同一套云能力可复用到多个产品
- 让自托管与官方托管共享同一套核心模型

## 阶段目标

### Phase 1

状态：`Implemented`

- 文档真源与工作区骨架
- Rust server/cli/crates 结构

### Phase 2

状态：`In Progress`

- 本地文件系统 backend
- challenge + token
- WebDAV 接口
- `gaubee-2fa` app 接入验证

### Phase 3

状态：`Planned`

- `S3` backend
- 在线授权流程
- 更通用的 app/token 管理
