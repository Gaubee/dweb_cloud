# Replace cloud.example.com with your real domain before use.

cloud.example.com {
	encode zstd gzip

	request_body {
		max_size 32MB
	}

	reverse_proxy dweb-cloud:9080 {
		header_up Host {host}
	}
}
